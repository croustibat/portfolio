{
  "name": "baptiste-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:lab": "npm run build:lab && astro dev",
    "generate:ascii": "node scripts/generate-ascii.mjs",
    "build:lab": "[ -d lab ] && cd lab && npm install && npm run build || true",
    "prebuild": "npm run generate:ascii && npm run build:lab",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@vercel/analytics": "^1.6.1",
    "astro": "^5.16.9",
    "sharp": "^0.34.4",
    "tailwindcss": "^4.1.18"
  }
}
